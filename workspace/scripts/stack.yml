name: henbot_stack
root: ~/workspace

startup_window: main

attach: false

windows:
  - core:
      - roscore
  - main:
      layout: tiled
      pre: sleep 5
      panes:
      - rosrun jetbot_ros jetbot_camera

        

